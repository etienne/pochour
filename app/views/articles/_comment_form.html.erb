<%= simple_form_for [@article.user, @article, @article.comments.build] do |f| %>
  <% default_name = current_user.name if user_signed_in? -%>
  <%= f.input :author, :input_html => { :value => default_name } %>
  <%= f.input :comment %>
  <%= f.submit %>
<% end %>
